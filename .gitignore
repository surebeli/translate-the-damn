## .NET / build output
bin/
obj/
[Dd]ebug/
[Rr]elease/
x64/
x86/
*.user
*.suo
.vs/

## Rider / VS Code
.idea/
.vscode/

## NuGet
*.nupkg
packages/

## OS
Thumbs.db
.DS_Store

## App runtime artifacts (never commit user config / secrets)
*.log
local.config.json
config.json
**/.translatethedamn/
*.config.json.bak
*.bak

## Vendor tool artifacts (mimo plan files, etc.)
.mimocode/

## Local issue tracking (not tracked by repo)
.local-issues/

# Python bytecode
__pycache__/
*.pyc

# per-platform conformance results emitted by runners for parity-verify (mechanism #9) — CI artifact, not source
conformance-results.*.json

## visual-walkthrough scratch shots
.shots/

## Local promo videos / recordings — keep out of git (bloat). For README playback, upload to a
## GitHub Release / attachment and embed the CDN URL (inline player), not a repo path.
docs/assets/recordings/
